# Handoff

Updated: 2026-05-22T08:44:00.000Z

PubPaid Xadrez mobile corrigido no build `20260522-chessmobile3`.

- Intro do Xadrez agora redesenha por fase (`Prepare-se`, creditos e moeda), porque a chave de renderizacao passou a incluir `chessIntroPhase`/`chessIntroCredits`.
- Mobile horizontal validado com 64 casas, 32 pecas, `outCount=0`, instrucao `Use toque na mesa e pinca para zoom`, apenas `Mesa fixa` visivel e jogada real por toque/click (`b8` para `c6`).
- Guard PubPaid e `node --check` passaram localmente.

## Next

- Commitar e subir online.
